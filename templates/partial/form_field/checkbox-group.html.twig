<div id="{{ id }}" class="sp-checkbox-group-wrap js-form-element">
    {% if label %}
        <label class="sp-checkbox-group-label">
            {{ label }}
        </label>
    {% endif %}
    {% for option in options %}
        <label class="sp-checkbox-label">
            {{ option.label }}
            <input id="{{ option.id }}" class="sp-checkbox" type="checkbox" value="{{ option.value }}">
        </label>
    {% endfor %}
</div>
