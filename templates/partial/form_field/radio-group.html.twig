<fieldset id="{{ id }}" class="{{ classes }}">
    {% if label %}
        <label class="{{ label.classes }}">
            {{ label.value }}
        </label>
    {% endif %}
    {% for option in options %}
        <label class="sp-radio-label">
            {{ option.label }}
            <input id="{{ option.id }}" class="{{ option.classes }}" type="radio"
                   value="{{ option.value }}" name="{{ id }}">
        </label>
    {% endfor %}
</fieldset>
