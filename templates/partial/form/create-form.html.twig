{% if title %}
    <h3>{{ title }}</h3>
{% endif %}
<form id="{{ id }}" action="{{ action }}" method="post">
    {% if fields is not empty %}
        {% for field in fields %}
            {{ field.content }}
        {% endfor %}
    {% endif %}
</form>
